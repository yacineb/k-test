{
  "name": "koober-interview",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index.js",
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "test": "jest -c jest.unit.config.js --forceExit --verbose --detectOpenHandles --color",
    "test:integration": "jest -c jest.integration.config.js --forceExit --verbose --detectOpenHandles",
    "init": "tsc --init",
    "build": "tsc",
    "start": "dist/index.js"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.1",
    "jest": "^26.4.2",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  }
}
