{
  "name": "koober-interview",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index.js",
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "test": "jest -c jest.unit.config.js --forceExit --verbose --detectOpenHandles --color",
    "test:integration": "rm -rf ouptut.json && jest -c jest.integration.config.js --forceExit --verbose --detectOpenHandles",
    "build": "tsc",
    "build:watch": "tsc -w",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "jest": "^26.4.2",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  }
}
